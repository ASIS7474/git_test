<!DOCTYPE html>
<html>
<head>
    <title>Next/Submit Button Example</title>
</head>
<body>

<div id="Next">Message will appear here</div>
<button id="actionBtn">Next</button>

<script>
    function handleNextButtonClick() {
    // Submit prompt values
    cognos.Report.getReport().prompt.getControlByName('MyPrompt').sendRequest();
    // Navigate to the next page
    cognos.Report.getReport().sendRequest(cognos.Report.Action.NEXT);
    cognos.Report.getReport().sendRequest(cognos.Report.Action.handleRepromptButtonClick);
}
 
// Attach event handlers to the "Next" and "Reprompt" buttons
document.getElementById('nextButton').addEventListener('click', handleNextButtonClick);
</script>
</body>
</html>
